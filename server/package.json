{
  "name": "assignment-planner-server",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test ./tests/*.test.mjs",
    "playwright:install": "playwright install chromium",
    "render-build": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "fetch-cookie": "^3.0.1",
    "node-fetch": "^3.3.2",
    "playwright": "^1.59.1",
    "tough-cookie": "^4.1.3"
  }
}
