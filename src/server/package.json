{
  "name": "assignment-planner-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test ./tests/*.test.mjs",
    "playwright:install": "playwright install chromium",
    "render-build": "cd ../.. && npm install && npx playwright install chromium"
  }
}
