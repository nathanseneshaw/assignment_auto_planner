{
  "name": "assignment-planner-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test ./tests/*.test.mjs",
    "render-build": "cd ../.. && npm install"
  }
}
