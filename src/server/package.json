{
  "name": "plannr-server",
  "private": true,
  "type": "module",
  "//": "Minimal manifest whose only job is to declare type:module so Node loads the .js files as ESM. Dependencies are listed in the root package.json (Render's Dockerfile copies that one in and runs `npm ci` against it). Do not add a `dependencies` block here."
}
